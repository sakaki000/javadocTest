name: Javadoc Check
on: workflow_dispatch
#  push:
#    branches: [ main_** ]
#    paths: 
#    - 'first/src/**'
#    - 'second/src/**'

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Echo Value
        run: |
          token=${ GITHUB_ACTOR }
          echo $test
